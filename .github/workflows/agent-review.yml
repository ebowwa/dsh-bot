# dsh-bot: agent-review — the adversarial review stage as a REUSABLE workflow.
# The consumer keeps a thin shell (on: workflow_dispatch + issue_comment
# /review, the trust gate); this holds the whole review job.
#
# Runs in the CALLER's context (caller's runners, GITHUB_TOKEN, secrets).
#
# Inputs:
#   rules-file   repo-relative path of the review rules doc. Default REVIEW.md
#   timeout-minutes  default 45

name: agent-review

on:
  workflow_call:
    inputs:
      rules-file:
        description: "Review rules document the reviewer must ground itself in"
        type: string
        default: "REVIEW.md"
      model:
        description: "Model for this run: provider/model (e.g. zai/glm-5.2). Empty = fleet default"
        type: string
        default: ""
      subagent-model:
        description: "Model for the subagent/subagent_fork children: provider/model (e.g. zai/glm-5-turbo). Empty = inherit the head's model"
        type: string
        default: ""
      timeout-minutes:
        description: "Per-review cap in minutes"
        type: number
        default: 45
      dsh-bot-ref:
        description: "Git ref of ebowwa/dsh-bot to fetch scripts from"
        type: string
        default: ""
      # Comma/JSON labels the review job runs on (default the dsh pool).
      # Callers whose runners carry different labels pass their own.
      runner-labels:
        type: string
        default: '["self-hosted","dsh"]'
      pr:
        description: "PR number to review (workflow_dispatch callers pass this)"
        type: string
        default: ""

jobs:
  dsh-review:
    runs-on: ${{ fromJSON(inputs.runner-labels) }}
    timeout-minutes: ${{ inputs.timeout-minutes }}
    steps:
      - name: Resolve PR number
        id: pr
        env:
          EVENT_PATH: ${{ github.event_path }}
          GH_TOKEN: ${{ github.token }}
          # Dispatch input reaches bash through env, never raw workflow
          # interpolation into the run block (review finding 3 — same
          # hygiene class as the task/comment-text seam).
          PR_INPUT: ${{ inputs.pr }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            N="$PR_INPUT"
          else
            N="$(node -e '
              const e = JSON.parse(require("fs").readFileSync(process.env.EVENT_PATH, "utf8"));
              const m = (e.comment?.body ?? "").match(/^\/review[[:space:]]*#?(\d+)/);
              process.stdout.write(m ? m[1] : String(e.issue?.number ?? ""));
            ')"
          fi
          echo "number=$N" >> "$GITHUB_OUTPUT"
          # State pre-flight: the tower can dispatch a review for a PR that
          # merged moments earlier (dsh-bot #27, run 32936033264) — GitHub
          # deletes refs/pull/N/merge when a PR merges, so the checkout below
          # died with "couldn't find remote ref refs/pull/27/merge" (3 git
          # retries) and the job went red on a race nobody could act on.
          # Resolve the state here; MERGED/CLOSED PRs skip review and the
          # relay step says why. LOOKUP_FAILED (no gh on this runner, bogus
          # number) proceeds as before — the checkout surfaces that error.
          #
          # --repo is REQUIRED (review finding 1): this is the job's first
          # step, no checkout has run, the workspace has no .git, and gh
          # resolves the repo from the cwd's git remote — it does NOT read
          # GITHUB_REPOSITORY. Without --repo every clean-workspace dispatch
          # silently degraded to LOOKUP_FAILED and the skip path was dead
          # code. gh's stderr is kept and surfaced on the fallback, not
          # swallowed — a bare redirect here is what hid this failure mode.
          STATE_ERR="$RUNNER_TEMP/pr-state-stderr.txt"
          STATE="$(gh pr view "$N" --repo "$GITHUB_REPOSITORY" --json state --jq .state 2>"$STATE_ERR" || true)"
          if [ -z "$STATE" ]; then
            STATE="LOOKUP_FAILED"
            echo "::warning::PR state lookup failed for #$N — proceeding as before. gh stderr: $(tr '\n' ' ' < "$STATE_ERR" 2>/dev/null || echo none)"
          fi
          echo "state=$STATE" >> "$GITHUB_OUTPUT"
          echo "PR $N state: $STATE"

      - name: Checkout the PR merge ref
        if: steps.pr.outputs.state != 'MERGED' && steps.pr.outputs.state != 'CLOSED'
        uses: actions/checkout@v4
        with:
          ref: refs/pull/${{ steps.pr.outputs.number }}/merge
          fetch-depth: 0

      - name: Fetch dsh-bot scripts
        if: steps.pr.outputs.state != 'MERGED' && steps.pr.outputs.state != 'CLOSED'
        uses: actions/checkout@v4
        with:
          repository: ebowwa/dsh-bot
          # dsh-bot-ref when given; else MAIN — never github.ref: the
          # CALLER may fire this workflow from a target repo whose default
          # branch is not main (ANE: dev), and dsh-bot has no such branch —
          # the fetch died with "couldn't find remote ref refs/heads/dev"
          # on every cross-repo tower review (observed 2026-08-24).
          ref: ${{ inputs.dsh-bot-ref || 'main' }}
          path: .dsh-bot
          token: ${{ secrets.DSH_BOT_REPO_TOKEN || github.token }}

      - name: Review the PR (rules + gates + verdict)
        if: steps.pr.outputs.state != 'MERGED' && steps.pr.outputs.state != 'CLOSED'
        env:
          DOPPLER_SERVICE_TOKEN: ${{ secrets.DOPPLER_SERVICE_TOKEN }}
          GH_TOKEN: ${{ github.token }}
          PR: ${{ steps.pr.outputs.number }}
          RULES_FILE: ${{ inputs.rules-file }}
          DSH_MODEL: ${{ inputs.model }}
          DSH_SUBAGENT_MODEL: ${{ inputs.subagent-model }}
          DSH_BOT_DIR: ${{ github.workspace }}/.dsh-bot
        run: |
          set -euo pipefail
          MASK_PROBE="DSH-REVIEW-MASK-9c2b"
          echo "::add-mask::${MASK_PROBE}"
          command -v doppler >/dev/null 2>&1 \
            && doppler run --token "$DOPPLER_SERVICE_TOKEN" -- \
                 sh -c 'printf "::add-mask::%s\n" "$ZAI_API_KEY"' || true

          cat > "$RUNNER_TEMP/review-task.txt" <<'TASKEOF' 
          You are the REVIEW stage of this repo's agent loop. Your job is to
          find what the author missed — assume the PR is guilty. You are
          reviewing a change by an agent like you with no sunk cost: be
          adversarial and specific.

          PR number: __PR__
          Ground truth, read both first:
            - ${RULES_FILE}  (the review rules; every finding cites a rule)
            - CLAUDE.md  (the architecture contract; skip silently if absent)

          Method, in order:
          1. gh pr view __PR__ --json title,body,files — read the claims.
          2. git diff origin/main...HEAD — read the whole diff.
          3. Open every changed file in full; read surrounding context.
          4. Run the gates yourself if the repo has them (typecheck/test/arch
             or equivalents; discover from package.json). Record results.
          5. Check tests actually cover the behavior change.
          6. Check the PR description against reality — overstatement is a
             blocking defect, same as a bug.

          Verdict — submit exactly one GitHub review and nothing else:
            gh pr review __PR__ --request-changes --body-file <file>
          or, only if you found nothing blocking:
            gh pr review __PR__ --approve --body-file <file>
          (A bot cannot approve its own PR — if the API rejects approve with
          "can not approve your own pull request", resubmit the same body as
          --comment and say so at the top.)

          Review body format (markdown):
            ## Verdict: APPROVE or REQUEST CHANGES
            ## Gates (each: pass/fail, with the failing bits)
            ## Findings (numbered; each cites the rule; file:line and a
               concrete suggested fix; severity: blocking/minor)
            ## Description accuracy (claims vs reality)

          Then set exactly one label:
            gh pr edit __PR__ --add-label ai-reviewed        (approve)
            gh pr edit __PR__ --add-label changes-requested  (request changes)
          (Remove the other label if present: --remove-label.)

          Rules for yourself: do NOT merge, do NOT push, do NOT comment on
          the thread (the review IS the output). Never print secrets, IPs,
          or absolute runner paths. Finish with a one-paragraph summary.
          TASKEOF
          TASK="$(sed "s/__PR__/$PR/g" "$RUNNER_TEMP/review-task.txt")"

          export REPLY_TARGET=""   # review mode: no thread reply, no shipping
          bash "$DSH_BOT_DIR/scripts/run-dsh-agent.sh" "$TASK" \
            | node "$DSH_BOT_DIR/scripts/scrub-output.mjs" \
            | tee "$RUNNER_TEMP/dsh-review-output.txt"

      - name: Relay review summary to the thread
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          PR: ${{ steps.pr.outputs.number }}
          PR_STATE: ${{ steps.pr.outputs.state }}
          DSH_BOT_DIR: ${{ github.workspace }}/.dsh-bot
        run: |
          set -euo pipefail
          # gh may sit outside the runner service PATH on self-hosted cells
          # (secondsee lane-lottery, 2026-08-26): probe the driver's
          # persistent prefix + brew prefixes before giving up. A missing gh
          # here degrades to a warning — the verdict itself is in the run
          # log (tee'd by the review step), and this step's failure would
          # only bury it under a bare 127. The list mirrors the driver's
          # CELL_PROBE_DIRS exactly (doppler prefix + linuxbrew included)
          # and honors its DSH_CELL_BIN seam.
          command -v gh >/dev/null 2>&1 \
            || export PATH="${DSH_CELL_BIN:-${HOME:-/root}/.dsh-bot-bin}:/opt/homebrew/bin:/usr/local/bin:$HOME/.doppler/bin:/home/linuxbrew/.linuxbrew/bin:$PATH"
          command -v gh >/dev/null 2>&1 || {
            echo "::warning::gh unavailable on this cell — review summary NOT relayed to the thread; see the run log"
            exit 0
          }
          {
            echo "**dsh review (GLM-5.3)** — run: ${GITHUB_RUN_ID} — lane: ${RUNNER_NAME:-unknown}"
            echo
            if [ "$PR_STATE" = "MERGED" ] || [ "$PR_STATE" = "CLOSED" ]; then
              echo "_(skipped — PR is $PR_STATE; the merge ref no longer exists, there is nothing left to review)_"
            elif [ -f "$RUNNER_TEMP/dsh-review-output.txt" ]; then
              node "$DSH_BOT_DIR/scripts/scrub-output.mjs" < "$RUNNER_TEMP/dsh-review-output.txt" 2>/dev/null \
                || echo "_(review output withheld: scrubber unavailable)_"
            else
              echo "_(no review output was produced — see the failed step above)_"
            fi
          } > "$RUNNER_TEMP/dsh-review-reply.md"
          gh pr comment "$PR" --repo "$GITHUB_REPOSITORY" --body-file "$RUNNER_TEMP/dsh-review-reply.md"
